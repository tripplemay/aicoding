services:
  app:
    build: .
    env_file: .env.production
    environment:
      DATABASE_URL: file:/data/app.db
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - sqlite_data:/data
    restart: unless-stopped

volumes:
  sqlite_data:
